import React from 'react';

import __COMPONENT__ from '.';

it('should render a __component__', () => {
  const wrapper = shallow(
    <__COMPONENT__ color="#0082c8" />,
  ).dive();
  expect(wrapper.props().color).toEqual('#0082c8');
  expect(wrapper).toMatchSnapshot();
});
